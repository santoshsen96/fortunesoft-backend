# Wookie Movies — MERN Assignment

## Backend
- Import `movie_db.json` using mongoimport into DB `fortunesoft` collection `movies`.
- Start server:
  - set mongoURL in .env
  - npm install
  - npm run server
- Endpoint: GET /api/movies
- Auth: Bearer FSMovies2023 (In Headers)


## Notes
- The API formats movies grouped by category (genres).
- README contains the commands used to import DB.
